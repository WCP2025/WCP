import { Hero } from '@/components/Hero';

/** Sección "Home": por ahora es solo el Hero, pero queda como su
 * propio bloque en /sections por si más adelante se agrega más
 * contenido introductorio antes de "Nosotros". */
export function Home() {
  return <Hero />;
}
